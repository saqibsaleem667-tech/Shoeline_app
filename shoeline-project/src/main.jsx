import React from "react";
import ReactDOM from "react-dom/client";
import ShoelineApp from "./App.jsx";
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ShoelineApp />
  </React.StrictMode>
);
